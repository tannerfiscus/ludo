import React from 'react';

import Layout from '../layout';

const Game = ({ gameId }) => (
    <Layout isFullWidth>
        <br/><br/><br/>
    <p>game play: {gameId}</p>
    </Layout>
);

export default Game;